services:
  - type: web
    name: ZenHealth-App
    env: python
    pythonVersion: 3.11
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
